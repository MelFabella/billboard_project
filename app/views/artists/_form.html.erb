<h1><%= @billboard.id ? 'Edit Billboard' : 'Create Billboard' %></h1>
<hr />
<%= form_for @billboard do |f| %> 
  <%= f.label :title %> 
  <%= f.text_field :title, required: true, autofocus:true %>
  <br />
  <%= f.label :rank %>
  <%= f.rank_area :rank, required:true %> 
  <%= f.submit %> 
<% end %> 