
  <%= form_for @billboard do |f| %>

<%#= form_with(model: @billboard, local: true, class: 'styled form') do |f| %>
  <%= f.label :title %> 
  <%= f.text_field :title, required: true, placeholder: 'billboard title' %>

  <%= f.submit %>
<% end %>